<h1 class="text-center text-dark" style="font-family: 'Caveat', cursive;">Browse Libaries</h1>
<div class="d-flex justify-content-center bg-dark p-2 bg-opacity-25 shadow mb-5 rounded">
  <div style="background-color: rgba(245, 235, 215, 0.1);">
    <h4 class="text-left p-3 text-dark" style="font-family: 'Comfortaa', cursive;">Click on a library for more details.</h4>
    <div class="d-flex justify-content-center align-items-center container row row-cols-1 row-cols-md-3 g-4" style="font-family: 'Comfortaa', cursive;">
      <!-- Here's where our Get All Libraries results will live. -->
        {{#each libraries as |library|}}
        <div class="col d-flex justify-content-center">
          <div class="card text-dark shadow mb-5" style="background-color: #F5EBD7;">
            <div class="card-body">
              <h5 class="card-title text-dark p-3">{{library.name}}</h5>
              <p class="card-text px-3">{{library.address}}</p>
              <p class="card-text px-3">Zip Code: {{library.zip_code}}</p>
            </div>
            <div class="card-body d-flex justify-content-center p-2">
              <button class="btn btn-outline-dark shadow my-3 mb-3 rounded mh-10" type="button" onclick="location.href='/libraryinfo/{{library.id}}'" style="background-color: #A6B7BE;">View Library Books</button>
            </div>
          </div>
        </div>
        {{/each}}
  </div>
  </div>
</div>